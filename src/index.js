import React from 'react';
import ReactDOM from 'react-dom';
import DesignerNews from './DesignerNews';

ReactDOM.render(
  <DesignerNews headerText="SapientNitro DesignerNews "/>,
  document.getElementById('root')
);
